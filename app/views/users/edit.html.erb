<div class ='container'>
    <div class ='row'>
        <div class ='col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto'>
            <h2>User info</h2>
            
           <%= form_with model:@users, url: '/users', method: :post, local:true do |f| %>
           
            <%= render 'layouts/errors', obj: @user %>
            
            <div class="form-group">
              <%= f.label :name %>
              <%= f.text_field :name, :value =>"#{@user.name}", class: "form-control name" %>
            </div>
            
            <div class="form-group">
              <%= f.label :image %>
     １}", class: "form-control-file user_image", accept: 'image/*' %>
            </div>
            
            <div class="form-group">
              <%= f.label :introduction %>
              <%= f.text_area :introduction, :value =>"#{@user.introduction}", class: "form-control introduction" %>
            </div>
            
            <div class="form-group">
              <%= f.submit "Update User", class: 'btn btn-secondary' %>
            </div>
            
           <% end %>
        </div>
    </div>
</div>